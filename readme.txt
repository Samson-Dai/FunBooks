I only have one html file, i.e. index.html. In order to display different pages, I use many <div>, which will display/hide according to the controller. I only use one app and one controller to controll all the pages.

One user can purchase at most 30 copies for one book at one time. And in the shoping cart, the quantity for each book cannot exceed 100.